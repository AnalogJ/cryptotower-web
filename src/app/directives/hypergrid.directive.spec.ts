import { HypergridDirective } from './hypergrid.directive';

describe('HypergridDirective', () => {
  it('should create an instance', () => {
    const directive = new HypergridDirective();
    expect(directive).toBeTruthy();
  });
});
